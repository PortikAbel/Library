version: '3'
services:
  api:
    build: api/
    ports:
      - "5000:5000"
    links:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
      
  web:
    build: web/
    ports:
      - "3000:3000"